# CLAUDE.md

คำแนะนำสำหรับ Claude เวลาทำงานในโปรเจกต์นี้

## ภาพรวมโปรเจกต์

เว็บฟอร์มสั่งคัสตอมคอนโทรลเลอร์ PS4 (ร้าน Controller Mod by Mosu) — ฐานข้อมูลทั้งหมดอยู่ใน Google Sheets เชื่อมผ่าน Google Apps Script Web App ไม่มี framework/build step ใดๆ เป็น static HTML ล้วนๆ

## 🚧 งานที่กำลังทำอยู่ — ระบบ login ลูกค้า + "คำสั่งซื้อของฉัน"

สถานะ: **เขียนโค้ดเสร็จแล้วทั้งฝั่งหน้าเว็บและฝั่ง Apps Script (ยังไม่ได้ deploy จริง)**

- `index.html` เพิ่มปุ่ม "เข้าสู่ระบบ" (top-bar), หน้าจอเข้าสู่ระบบ/สมัครสมาชิก, หน้า "คำสั่งซื้อของฉัน", หน้า "ลืมรหัสผ่าน"/"ตั้งรหัสผ่านใหม่" แล้ว — ไม่บังคับ login ก่อนสั่งซื้อ (guest checkout ยังทำงานเหมือนเดิมทุกประการ)
- `apps-script-order-receiver.gs` เพิ่มชีต Users/Sessions/PasswordResets + action `register`/`login`/`logout`/`getMyOrders`/`forgotPassword`/`resetPassword` แล้ว — ดูรายละเอียดที่ [`CHANGELOG.md`](CHANGELOG.md) หัวข้อล่าสุด
- **ค้างอยู่ก่อน deploy จริง**: ยังไม่ได้ตัดสินใจว่าจะเปิดให้สมัครสมาชิกอิสระแบบนี้จริงไหม หรืออยากได้ระบบรหัสเดียวใช้ร่วมกันแทน (ถามผู้ใช้ก่อนถ้ายังไม่ชัดตอนเริ่ม deploy)
- **ต้องทำก่อนใช้งานจริง**: Deploy `apps-script-order-receiver.gs` เวอร์ชันใหม่ (ดูขั้นตอนใน "ข้อควรระวัง" ด้านล่าง) + ตั้งค่า `SITE_URL` ในไฟล์ `.gs` ให้เป็น URL จริงของ `index.html` (ลิงก์รีเซ็ตรหัสผ่านในอีเมลใช้ค่านี้) — ปุ่ม login/สมัครสมาชิก/คำสั่งซื้อของฉันจะไม่ทำงานจนกว่าจะ deploy
- ลบ section นี้ทิ้งเมื่อ deploy จริงแล้วและทดสอบผ่าน (สรุปอยู่ใน [`CHANGELOG.md`](CHANGELOG.md) แล้ว)

### ไฟล์หลัก
- **`index.html`** — หน้าเว็บลูกค้า (ฟอร์มสั่งซื้อ) นี่คือไฟล์ที่ใช้งานจริง (เดิมชื่อ `index (9).html` เปลี่ยนชื่อแล้ว — ไฟล์ใน `backup/` เป็นเวอร์ชันเก่า)
- **`dashboard.html`** — แผงควบคุมแอดมิน (กำไร / ราคา-สินค้า / ออเดอร์) ป้องกันด้วยรหัสผ่าน ไม่ได้ลิงก์จากหน้าเว็บลูกค้า
- **`apps-script-order-receiver.gs`** — backend ทั้งหมด (Google Apps Script) อ่าน/เขียน Google Sheets โดยตรง โค้ดในไฟล์นี้เป็นแค่ **สำเนาอ้างอิงในเครื่อง** — ของจริงรันอยู่บน Google Apps Script Editor ต้อง deploy ทุกครั้งที่แก้ไฟล์นี้ (ดูหัวข้อ "ข้อควรระวัง" ด้านล่าง)
- **`DS4 COLOR/`** — รูปสีกรอบคอนโทรลเลอร์ (ใช้ในตัวเลือก "ไม่มีจอย ให้เลือกสี")

### ฐานข้อมูล (Google Sheets)
ชื่อชีต: `Prices` (ราคา/ต้นทุน/สต๊อก/เปิดขาย), `Orders` (คำสั่งซื้อ), `Presets` (แพ็กเกจสำเร็จรูป), `AnalogService` (ราคาโหมดเปลี่ยนอนาล็อกอย่างเดียว), `Settings` (เปิด/ปิดรับออเดอร์แต่ละโหมด), `Discounts` (โค้ดส่วนลด) — ทุกชีตสร้าง/เพิ่มคอลัมน์ให้อัตโนมัติเองได้ (self-healing) ไม่ต้องสร้างมือ

## ข้อควรระวัง

- **ไฟล์ `.gs` ในเครื่องกับโค้ดที่รันจริงบน Apps Script อาจไม่ตรงกัน** — เคยเจอมาแล้วว่าชีต Orders จริงมีชื่อคอลัมน์ต่างจากที่โค้ดคาดไว้ (ของเก่าที่ตั้งไว้ก่อนหน้า) ก่อนแก้ logic ที่ผูกกับชื่อคอลัมน์ตรงๆ ให้คิดถึงความเป็นไปได้นี้ไว้เสมอ ใช้การจับคู่แบบยืดหยุ่น (ดูตัวอย่าง `ORDER_COLUMN_ALIASES` / `pickField()`) แทนการ hardcode ชื่อคอลัมน์เดียว
- แก้ไฟล์ `.gs` แล้ว **ต้องบอกผู้ใช้ให้ deploy ใหม่เสมอ** (Apps Script Editor → Deploy → Manage deployments → แก้ deployment เดิม → Deploy) ไม่งั้นการเปลี่ยนแปลงจะไม่มีผลจริงบนเว็บ/แดชบอร์ด
- `dashboard.html` ใช้รหัสผ่านเดียว (`DASHBOARD_PASSWORD` ในไฟล์ .gs) ป้องกันทุก action ของแอดมิน — ไม่ใช่ระบบ login เต็มรูปแบบ อย่าลดระดับความปลอดภัยตรงนี้โดยไม่ถามก่อน
- ฟิลด์ที่กระทบราคาสินค้า/สต๊อก/การมองเห็นตัวเลือก ควรทดสอบทั้งฝั่งแสดงผล (`index.html`) และฝั่งตรวจสอบฝั่งเซิร์ฟเวอร์ (`runAntiSpamChecks`/`findSoldOutSelections` ใน .gs) เพราะทั้งสองฝั่งต้องรู้จักตัวเลือกใหม่ตรงกัน

## การทดสอบ

ไม่มี dev server/build — เปิดไฟล์ HTML ตรงๆ ผ่าน Browser tool (`file://...`) ทดสอบ flow จริงด้วย JS injection (`javascript_tool`) แทนการ screenshot เพราะ preview pane ของ sandbox นี้บางทีไม่ยอม compositing เฟรม และการ `navigate force:true` ซ้ำๆ ในแท็บเดิมของไฟล์ local อาจไม่รีเซ็ต JS state จริง (เจอมาแล้วหลายรอบ) — ถ้าผลลัพธ์ดูค้าง/ไม่สมเหตุสมผล ให้สงสัยปัญหานี้ก่อน แล้วทดสอบ logic แบบ isolated (เรียกฟังก์ชันตรงๆ ด้วยข้อมูลจำลอง) แทน

## Changelog

**หลังจบงานทุกครั้ง (ทุก session ที่แก้โค้ดในโปรเจกต์นี้) ให้เพิ่มบันทึกใหม่ไว้บนสุดของ [`CHANGELOG.md`](CHANGELOG.md)** สรุปว่าเปลี่ยนอะไร/ทำไม/ต้องทำอะไรต่อ (เช่น "ต้อง deploy ใหม่") แบบสั้นกระชับเป็น bullet ไม่ต้องเขียนพรรณนายาว ทำเสมอโดยไม่ต้องรอให้ผู้ใช้สั่ง

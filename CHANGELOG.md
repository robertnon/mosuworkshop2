# Changelog — Controller Mod by Mosu

บันทึกการเปลี่ยนแปลงของโปรเจกต์นี้ เรียงจากล่าสุดไปเก่าสุด (ใหม่อยู่บนสุด) แต่ละรอบให้สรุปว่า **เปลี่ยนอะไร**, **ทำไม**, และ **ต้องทำอะไรต่อ** (ถ้ามี) แบบสั้นๆ อ่านง่าย ไม่ต้องเขียนยาว

---

## 2026-09-16 (รอบที่ 18) — ภาพ PCB เข้าไปในแถวตัวเลือก (ไอเดียลูกค้า)

### เปลี่ยนอะไร
- รูป SuiOvOi 8K / Hyperstrike 8K PCB ย้ายจากกล่องพรีวิวใหญ่เหนือแถว
  → **thumbnail 76×58px อยู่ในแถวตัวเลือก** (ข้าง radio, ก่อนชื่อ)
  เลือกแล้ว frame thumbnail กลายเป็นไลม์ + glow เล็ก
- ตัด wrapper `.pcb-preview-box` + อีโมจิ 🔲 ออกจากชื่อ SuiOvOi (รูปจริงมาแทน)
- **JS ปลอดภัย**: img ยังอยู่ใน `.pcb-col` เดิม + class `pcb-preview-img`
  คงอยู่ → `radioInput.closest('.pcb-col').querySelector('.pcb-preview-img')`
  (อัปเดตพรีวิวบอร์ด) + soldout hide ทั้งคอลัมน์ ทำงานต่อได้

### หมายเหตุ deployment
- อัป `index.html` ตัวเดียว (HTML: 2 บล็อก pcb-col + CSS: .pcb-thumb ใน SELECT LAYER)

---

## 2026-09-16 (รอบที่ 17) — หน้าตาการเลือกของใน wizard (ref: 3D configurator option rows)

### เปลี่ยนอะไร (เฉพาะ index.html — "เอาแค่หน้าตาที่เลือกของ" ไม่เอาสีธีมใหม่)
1. **ตัวเลือก = แถวเต็มความกว้าง** — `.options` / `.preset-grid` จาก grid
   หลายคอลัมน์ → แถวเดียวต่อกัน (title 15px 700 + sub 13px + ราคา mono ติดขวา
   เมื่อเลือกแล้วราคาสว่างเป็น copper-text)
2. **หัวข้อ step** — field-title 19px 700 + ช่องว่างชัดขึ้น (badge 00/01 + eyebrow คงไว้)
3. **Step tracker** — จากกล่อง pill → hairline + **progress bar ไลม์**
   (20→100% ตามขั้น) + ตัวเลข **"N OF 5"** ฝั่งขวา (pure CSS ผ่าน body:has()
   — บราวเซอร์ที่ :has ไม่ได้ จะค้าง "1 OF 5" เป็น graceful fallback)
4. **Next** — ปุ่มใหญ่ขึ้น (13px 34px, 15px 700)
- ไม่แตะ: 3D preview/layout คอลัมน์, สี/ธีม Fusion, radio/checkbox (JS ใช้),
  platform-select (ไอคอน PS4/PS5 คง 2 คอลัมน์)

### หมายเหตุ deployment
- อัป `index.html` ตัวเดียว (additive block "SELECT LAYER" ท้าย <style>
  — rollback = ลบบล็อกทั้งก้อน)

---

## 2026-09-16 (รอบที่ 15) — ธีม Fusion: Dark เป็นหลัก + ผสม Carbon + ล้างจุดๆ

### เปลี่ยนอะไร (ตามแผน "ใช้ dark เป็นหลัก แต่เอา carbon มาผสม แล้วเอาพื้นหลังจุดๆ ออก")
1. **พื้นผิวใหม่ (ทั้ง 2 ไฟล์: index + my-orders)** — surface ของธีมหลัก
   เปลี่ยนจากเขียวมะกอก → **เทาถ่านเย็นของ Carbon**:
   `--bg #090A0C / --panel #14171C / --panel-2 #1C2027 / --line #262B33 /
   --copper-dim #39414D / --silk #F0F3F6 / --silk-dim #858E9B`
2. **คงไว้ (เอกลักษณ์ dark/lime):** ไลม์แบรนด์ #C6FF00, แสง L&S ไลม์
   (--glow-rgb 198,255,0), corner glow 2 มุมบน (ยังใช้ --corner-glow-* →
   แผง ?tune=1 ยังทำงาน), haze + stage light
3. **ล้าง dot grid ทั้งหมด** — body ธีมหลัก + ธีม Carbon (ที่ toggle ถึง)
   ไม่มีจุดๆ แล้ว + ปิด animation gridMove
4. **Cyan #4FD8E8** ที่ตัวเลขราคารวม (float-summary-total / wizard-summary-total)
   = ลายเซ็น accent ของ carbon ที่ผสมเข้ามา (เฉพาะ index)

### สถานะธีมตอนนี้
- **Fusion (ค่าเริ่มต้น)** = carbon surface + lime glow/brand + cyan total + ไม่มีจุด
- **Carbon (ปุ่ม toggle)** = เดิม + ล้างจุดแล้ว (ต่างจาก fusion ตรงไม่มี corner glow)
- Gold / Light = legacy ซ่อน (toggle สลับไม่ถึง)

### หมายเหตุ deployment
- อัป `index.html` + `my-orders.html` (additive block "CARBON FUSION LAYER"
  ท้าย <style> — rollback = ลบบล็อกทั้งก้อน, ไม่มีอะไรแตะนอกจากนี้)

---

## 2026-09-16 (รอบที่ 14b) — Revise: ใช้ฟอนต์เดิม + เอารูปจอยออก

### เปลี่ยนอะไร (ตามคำขอ "ใช้ font เดิม เอารูปจอยออก" + "รูป controller เอาออกไปเลย")
1. **ฟอนต์เดิม** — ลบ Chakra Petch ออกจาก font link ทั้งหมด:
   หัวเรื่อง/ชื่อการ์ด = IBM Plex Sans Thai เดิม, เลข 01/02 = IBM Plex Mono
2. **ลบภาพจอย hero** + ไฟล์รูป — hero เป็น typography อย่างเดียว
3. **ลบ controller image ออกจาก mode cards ทั้ง 2 ใบ** (SVGDS4) → การ์ด text-only
   (JS ปลอดภัย: querySelectorAll('.mode-card-media') = list ว่าง)

### ยังคงไว้
- Direction เดิม: hero ซ้ายจัดเรียง, catalog cards 01/02, spec line, CTA ↗,
  hairline, ธีม L&S + Fusion ทั้งหมด

### หมายเหตุ deployment
- อัป `index.html` ตัวเดียว

---

## 2026-09-16 (รอบที่ 14) — Landing direction แบบ headglytch.com/presets

### เปลี่ยนอะไร
โจทย์: อยากได้หน้าเว็บประมาณ headglytch/presets ปรับให้เข้ากับเว็บ → เลือกขอบเขต "landing ก่อน"
(ไม่ clone ตรงๆ แต่เอาระบบภาพมาครอบ: hero ยักษ์ + การ์ดแคตาล็อกเลขลำดับ + UI เรียบ)

1. **Hero ใหม่** — ข้อความย้ายไปฝั่งซ้าย + ตัวอักษรยักษ์ (Chakra Petch display,
   clamp 38-62px) + eyebrow "// CONTROLLER MOD BY MOSU · BUILT FOR PROS BY PRO"
   (tagline เดิมของร้านจาก hero ฟอร์ม) + **ภาพจอยสตูดิโอ** ฝั่งขวา
   (`brand/hero-controller-studio.jpg` — generate ใหม่: จอยสองโทน matte,
   ไลม์ rim light, พื้นดำกลืนหน้าเว็บ, ไม่มีโลโก้) — บนมือถือภาพขึ้นด้านบน
2. **Mode cards = catalog 01/02** — เพิ่มเลขลำดับ (01 Custom Build / 02
   เปลี่ยนอนาล็อก), ชื่อเป็น display font 24px, desc เป็น spec line แบบ mono
   ตัวเล็ก, CTA เป็น uppercase + ลูกศร ↗ (สไตล์หัวglytch) + hairline
   คั่นระหว่าง hero กับ grid + media สูงขึ้น 230px
3. **ฟอนต์ Chakra Petch** กลับมาใน font link (ใช้เฉพาะ landing —
   หัวเรื่อง/เลขลำดับ/ชื่อการ์ด เท่านั้น)

### สิ่งที่ยังเป็นเหมือนเดิม
- ธีม Orbital L&S ทั้งหมด (แสง/เงา/ไลม์/มุมโค้งการ์ด) — landing layer
  วางต่อท้าย ทำงานร่วม (กรอบ hero ใช้ --glow-rgb/--edge-hi/--shadow-rest เดิม)
- ฟอร์มวิซาร์ด, my-orders, JS/flow/ราคา — ไม่แตะ (HTML ที่แก้ = ส่วน landing
  เท่านั้น, id/class ที่ JS ใช้คงครบ)

### หมายเหตุ deployment
- อัป `index.html` + **ไฟล์ใหม่ `brand/hero-controller-studio.jpg`** (65 KB)
- รูปใน mode cards (SVGDS4/*.svg) ยังเรียกเหมือนเดิม — ถ้า preview在这ดูรูปใน
  การ์ดไม่ขึ้น เพราะ workspace นี้ไม่มีโฟลเดอร์ SVGDS4 (เว็บจริงบน host มีปกติ)

### ต้องทำอะไรต่อ
- ถ้าถูกใจ direction: ขยายไป ฟอร์มวิซาร์ด + my-orders ในรอบถัดไป
- optional: strip "ต้องการงานเฉพาะทาง?" + ช่องทางติดต่อ (ต้องมีข้อมูล LINE/อีเมลก่อน)

---

## 2026-09-11 (รอบที่ 12) — ใช้ธีม "Orbital Light & Shadow" ตามสเปค THEME.md

### เปลี่ยนอะไร
ตามสั่ง: ใช้ theme จาก **`THEME.md`** (อยู่บน branch main) — ธีม "Orbital Light & Shadow"
ที่เขียนสเปคไว้ละเอียด (พาเลตต์เดิมทั้งหมด เปลี่ยนแค่ แสง/ขอบ/เงา/ความโค้ง)

**หมายเหตุ:** งาน ad-hoc 2 รอบก่อนหน้า (แสงสตูดิโอรอบ 10 + ชุด "หน้าต่างกระจก" รอบ 11)
ถูก **roll back ออกทั้งหมด** แล้วแทนที่ด้วยธีมตามสเปคนี้ ซึ่งออกแบบมาให้เป็น
บล็อก additive ที่ไม่แตะกฎเดิม

### โครงสร้าง (ตาม spec)
1. **ตัวแปรแกน** เพิ่มใน `:root` — `--glow-rgb` (สีแสงเรือง = สีแบรนด์, แยก RGB
   เพื่อใส่ alpha ได้), `--edge-hi` (ไฮไลต์ขอบบน), `--card-sheen` (แสงอาบบน→ล่าง),
   `--shadow-rest` / `--shadow-raised` (เงา 2 ชั้น) + override `--glow-rgb`
   ในธีม gold (ทอง 203,178,106) / carbon (ไลม์เดิม) → สลับธีมแสงเรืองเปลี่ยนสีเอง
2. **เวทีแสงพื้นหลัง** `body::after` — ไฟส่องจากบน + vignette มุมมืดลง
   (ซ้อนทับ dot grid + `--corner-glow-*` เดิม แผง `?tune=1` ยังปรับต่อได้)
3. **บล็อก `ORBITAL LIGHT & SHADOW LAYER`** ท้าย `<style>` (ค้นหา marker นี้ได้)
   recipe ราย component ตาม spec §7:
   - 7.1 การ์ดทั่วไป = กระจกยกตัว (sheen + ขอบ `rgba(var(--glow-rgb),0.20)` + ไฮไลต์ขอบบน + เงา 2 ชั้น)
   - 7.2 summary sticky = ขอบเรืองแสง (วงแหวน + แสงฟุ้ง)
   - 7.3 option/preset/platform = hover ลอย -2px / selected พื้นอมไลม์ + เรือง
     ของหมด (soldout) ห้ามเด้งห้ามเรือง
   - 7.4 input = "บ่อบุ๋ม" recessed + โฟกัสเป็นวงแสง (ไม่ทับลูกศร select)
   - 7.5 ปุ่มหลัก = ไล่แสง + เรืองใต้ตัว + กดยุบ
   - 7.6 ปุ่มรอง = แกะสลักตื้น + วงเรืองตอน hover
   - 7.7 pill/toggle = ร่องบุ๋ม + ก้อน active ยกเด่นเรือง (wizard/ภาษา/ธีม/3D view/spin/step dot)
   - 7.8 กล่องลอย (float summary/dropdown/tooltip) = เงาลึกสุด + วงเรือง
   - 7.9 badge/QR = มิติเล็กน้อย
   - §6 สเกลความโค้ง: 16/14/12/10/999px
4. **my-orders.html** ได้ตาม §9 (ตัวแปร + เวทีแสง + recipe order-card/badge/toggle)
5. ตัวเลข mono + `tabular-nums` (BOM/ราคา/summary wizard) ตาม §3

### สิ่งที่ยังเป็นเหมือนเดิม
- พาลเล็ตสี :root ทุกเฉด, ธีม gold/carbon, JS/logic/flow เลือกสินค้า — ไม่แตะ
- กฎเดิมทุกบรรทัดยังอยู่ครบ — diff เทียบ commit ก่อนหน้า = เพิ่มล้วน
  (แก้ 2 บรรทัดคือบรรทัด typography ที่เติมค่าต่อท้าย)

### ต้องทำอะไรต่อ
- deploy: อัป `index.html` + `my-orders.html` เข้า Apps Script
- **ไม่ต้อง**แก้ `?v=` ของ controller-3d*.js (ไม่ได้แตะไฟล์ JS)
- เผื่อใช้ธีมนี้กับหน้าอื่น: ดู THEME.md §9 (ก๊อป 3 ส่วน: ตัวแปร → เวทีแสง → จับคู่ recipe)

---

## 2026-09-11 (รอบที่ 9) — เพิ่มพรีวิว 3 มิติของ PS5 (DualSense)

### เปลี่ยนอะไร
เดิมเลือกรุ่น PS5 แล้ว**พรีวิวจะหายไปเลย** เพราะมีแต่โมเดล DS4 (PS4)
รอบนี้เพิ่มโมเดล DualSense เข้ามา ตอนนี้เลือกรุ่นไหนก็เห็นพรีวิว 3 มิติของรุ่นนั้น
หมุนดูได้ เปลี่ยนสีได้ เหมือนฝั่ง PS4 ทุกอย่าง

### โมเดลมาจากไหน
เอามาจาก **github.com/SafaElmali/dualsense-studio** ตามที่สั่ง
(ไฟล์ `controller/dualsense.glb` → เก็บเป็น `models/ds5.glb`)
เจ้าของโมเดลจริงๆ คือ **Taohid Animation** สัญญาอนุญาต **CC BY 4.0**
นอกจากตัวไฟล์แล้ว ยังยืม**ค่าวัสดุ** (ความมัน/ความหยาบของพลาสติกแต่ละชิ้น)
จาก `controller/controller-view.js` ของโปรเจกต์นั้นมาด้วย เพราะเขาจูนไว้ดีแล้ว

> **เครดิตจะสลับชื่อให้อัตโนมัติ** — เลือก PS4 ขึ้น "shaielwolf",
> เลือก PS5 ขึ้น "Taohid Animation" ทั้งคู่เป็น CC BY 4.0 ที่**บังคับ**ให้แสดงเครดิต
> ห้ามลบออก (รายละเอียดใน `models/ATTRIBUTION.txt`)

### เรื่องที่ต้องปรับให้เข้ากับร้าน
- **ลบโลโก้ PlayStation** ออกเหมือนที่ทำกับ DS4 — โมเดลนี้แยกโลโก้เป็นชิ้นต่างหาก
  เลยแค่ซ่อนชิ้นนั้น ง่ายกว่า DS4 มาก (ของ DS4 ต้องลบในเทกซ์เจอร์ทีละพิกเซล)
- **สีตั้งต้นเป็นสีขาว** ไม่ใช่สีดำ เพราะ DualSense ที่ขายทั่วไปเป็นสีขาว
  (ต่างจาก DS4 ที่เป็นสีดำ) ถ้าใช้ค่าเดียวกันพรีวิวจะไม่ตรงกับของจริง
- **ปุ่มปิดไมค์** ในไฟล์ต้นฉบับสว่างจนดูเหมือนโลโก้ PlayStation เลยทาสีเข้มลง
- **ทัชแพด** ในไฟล์ใช้วัสดุร่วมกับเชลล์ ถ้าปล่อยไว้จะเปลี่ยนสีตามเชลล์ไปด้วย
  ซึ่งไม่ตรงกับของจริง เลยแยกวัสดุออกมา
- **ปุ่มหน้า/D-pad** เดิมสีอ่อนจนกลืนกับเชลล์ขาว ปรับให้เข้มขึ้นพอมองเห็น

### ผลทดสอบ (ผ่านหมด ไม่มี error)

| สถานะ | ผล |
|---|---|
| ยังไม่เลือกรุ่น | ซ่อนพรีวิว + **ยังไม่โหลดไฟล์โมเดลเลย** |
| เลือก PS4 | โชว์ DS4 · เครดิต shaielwolf |
| กด "เปลี่ยนรุ่น" | ซ่อนพรีวิว |
| เลือก PS5 | โชว์ DualSense · เครดิต Taohid Animation |
| สลับไปมา PS4↔PS5 หลายรอบ | โมเดลถูกต้องทุกครั้ง ไม่ค้าง ไม่มี error |

เปลี่ยนสีเชลล์บน PS5 ได้จริงทุกสี และไล่เช็คของ PS4 ซ้ำทั้งชุด
(8 สี, บอร์ด PCB, stick module, trigger, ปุ่มหลัง) — ยังทำงานปกติ ไม่มีอะไรพัง

### สิ่งที่ต้องทำเอง
อัปโหลดไฟล์ใหม่ **`models/ds5.glb`** (6.2 MB) + **`controller-3d-ds5.js`**
และอัปทับ **`index.html`**, **`controller-3d.js`**, **`models/ATTRIBUTION.txt`**

---

## 2026-09-11 (รอบที่ 8) — ไลต์บาร์ของ "โมเดล 3 มิติ" เปลี่ยนเป็นสีขาว

### เข้าใจผิดรอบที่แล้ว — ขออภัยครับ
รอบที่ 6-7 ผมไปแก้ "รูปภาพตัวอย่างปุ่มหลัง" (ไฟล์ `*-paddles.png`)
แต่ที่ลูกค้าหมายถึงคือ **แถบไฟบนโมเดล 3 มิติที่หมุนได้** ซึ่งเป็นคนละที่กัน
รอบนี้แก้ให้ถูกจุดแล้ว (ของรอบที่แล้วยังคงไว้ เพราะรูปนั้นก็ควรเป็นขาวเหมือนกัน)

### ต้นตอ
สีไฟของโมเดล 3 มิติมาจากตัวแปร `PCB_LIGHT` ใน `controller-3d.js`
ซึ่งตั้งให้ไฟ**เปลี่ยนสีตามบอร์ด PCB** ที่ลูกค้าเลือก และ 2 ใน 3 ค่าเป็นโทนน้ำเงิน:

| สถานะ | สีเดิม | |
|---|---|---|
| ยังไม่เลือกบอร์ด | `#3A6BFF` | น้ำเงิน |
| suiovoi | `#27B6FF` | ฟ้า |
| hyperstrike | `#FF4D8D` | ชมพู |

(ตัวโมเดลเองมีไฟ "อบ" มาในไฟล์เป็นสีน้ำเงิน `#001dff` อยู่แล้วด้วย
โค้ดจะวาดเทกซ์เจอร์ทับเป็นสีที่เรากำหนด — เลยแก้ที่ `PCB_LIGHT` จุดเดียวพอ)

### แก้ยังไง
ตั้งเป็น **สีขาว `#FFFFFF` ทั้ง 3 ค่า**
ไฟยัง**สว่างขึ้นเมื่อเลือกบอร์ด**เหมือนเดิม (คุมด้วยความสว่าง ไม่ใช่สี)
ถ้าวันหลังอยากให้ไฟเปลี่ยนสีตามบอร์ดอีก แค่ใส่ค่าสีกลับเข้าไปในตัวแปรนี้

### ยืนยันผล
วัดจากเทกซ์เจอร์ไฟที่โค้ดวาดจริงในเบราว์เซอร์ (ไม่ใช่แค่ดูโค้ด):

| สถานะ | สีที่ส่งเข้า | สีเทกซ์เจอร์ที่ได้ |
|---|---|---|
| ยังไม่เลือกบอร์ด | `0xffffff` | **`#ffffff`** (จุดติดไฟ 67,758 px) |
| suiovoi | `0xffffff` | **`#ffffff`** |
| hyperstrike | `0xffffff` | **`#ffffff`** |

ไล่เช็คของเดิมซ้ำ ไม่พังสักอย่าง: กันพรีวิวโผล่ก่อนเลือกรุ่นยังทำงาน,
เปลี่ยนสีกรอบครบ 5 สี (เปลี่ยนจริง 5 เฉด), บอร์ด PCB, stick module,
trigger, ปุ่มหลัง — ผ่านหมด ไม่มี error

### สิ่งที่ต้องทำเอง
อัปโหลดทับ **`controller-3d.js`** และ **`index.html`** (เลข `?v=` ขยับเป็น 5 แล้ว)

---

## 2026-09-11 (รอบที่ 7) — แก้ปัญหา "ยังเห็นไลต์บาร์เป็นสีน้ำเงินอยู่" (รูปภาพ ไม่ใช่ 3 มิติ)

### ปัญหา
รอบที่ 6 เปลี่ยนสีในไฟล์รูปเรียบร้อยแล้ว แต่ลูกค้ายังเห็นเป็นสีน้ำเงินอยู่
**สาเหตุ: เบราว์เซอร์จำรูปเก่าไว้ (cache)** — รูป 3 ใบนี้ถูกเรียกแบบไม่มี `?v=`
เครื่องที่เคยเปิดเว็บตอนรูปยังฟ้า จะหยิบรูปเก่าในเครื่องมาโชว์ ไม่ยอมโหลดใหม่
(เป็นอาการเดียวกับรอบที่ 4 เป๊ะๆ ตอนนั้นเป็นไฟล์โมเดล 3 มิติ)

### แก้ยังไง
เติม **`?v=2`** ต่อท้ายชื่อรูปทั้ง 3 ใบในโค้ด เบราว์เซอร์จะมองว่าเป็นคนละไฟล์
แล้วโหลดของใหม่ทันที ลูกค้าไม่ต้องกด Ctrl+F5 เอง

> **แก้รูปพวกนี้ครั้งต่อไป อย่าลืมขยับเลขเป็น `?v=3`, `?v=4`, ...**

### พิสูจน์ว่าแก้ได้จริง
จำลองเครื่องลูกค้าที่เคยเปิดเว็บตอนรูปยังฟ้า (เปิด cache ไว้เหมือนเบราว์เซอร์จริง):

| ขั้นตอน | ผล |
|---|---|
| เปิดครั้งแรก ตอนรูปยังฟ้า ไม่มี `?v=` | เห็นสีฟ้า 1,086 จุด |
| สลับเป็นรูปขาว + ใส่ `?v=2` แล้วเปิดซ้ำ **โดยไม่ล้าง cache** | **เหลือสีฟ้า 0 จุด** |

### ตรวจเพิ่ม — ไล่ทั้งเว็บแล้วไม่เหลือสีน้ำเงินตรงไหนอีก
เดินครบทุกขั้นของฟอร์ม (6 ขั้น) สแกนหาสีน้ำเงินทุกจุดบนหน้าจอ — **ไม่พบเลย**
รวมถึงตรวจพรีวิว 3 มิติแยกอีกรอบทุกมุมกล้องและทุกตัวเลือกบอร์ด PCB
ก็ไม่มีสีน้ำเงินโผล่ (แถบไฟบนโมเดลตอนนี้ออกโทนเขียวตามแสงในฉาก)

### สิ่งที่ต้องทำเอง
อัปโหลดทับ **`index.html`** (รอบที่แล้วอัป 3 รูปไปแล้ว ถ้ายังไม่ได้อัปให้อัปด้วย)

---

## 2026-09-11 (รอบที่ 6) — เปลี่ยนแถบไฟ (ไลต์บาร์) ในรูปตัวอย่างปุ่มหลัง จากฟ้าเป็นขาว

### เปลี่ยนอะไร
รูปตัวอย่าง 3 รูปที่ใช้ในช่อง "ระดับความสูงของปุ่มหลัง"
(`standard-paddles.png`, `low-paddles.png`, `high-paddles.png`)
เป็นภาพถ่ายจอยด้านหลัง ซึ่ง**แถบไฟด้านบนเป็นสีฟ้า** — เปลี่ยนเป็น**สีขาว**แล้ว

### ทำไมต้องระวังตอนแก้
ไม่ได้ถมสีขาวทับทื่อๆ เพราะจะแบนดูเป็นสติกเกอร์แปะ
วิธีที่ใช้คือ **คงน้ำหนักแสงเงาเดิมของภาพถ่ายไว้** แล้วเปลี่ยนแค่เนื้อสี
ส่วนขอบที่ไล่เบลอกับพลาสติกดำก็ค่อยๆ ผสมสี (feather) กันขอบหยัก

### ผลลัพธ์
- ไม่เหลือพิกเซลสีฟ้าในทั้ง 3 รูป (นับได้ 0)
- พื้นหลังโปร่งใสยังอยู่ครบ ภาพไม่มีขอบขาวโผล่
- **ไฟล์เล็กลง ~33%** (รวม 3 รูป 774 KB -> 514 KB) หน้าเว็บโหลดเบาขึ้นนิดหน่อย
- ตรวจ CRC ทุก chunk แล้วไฟล์ไม่เสีย เปิดในเบราว์เซอร์ได้ปกติทั้ง 3 แบบ

### เผื่ออยากเปลี่ยนสีอีก
เพิ่มสคริปต์ **`tools/png-recolor-lightbar.py`** ไว้ให้
ถ้าอยากได้สีอื่น (หรืออยากปรับให้สว่าง/หม่นกว่านี้) แก้ค่าในส่วน CONFIG
ต้นไฟล์แล้วรัน `python3 tools/png-recolor-lightbar.py` ได้เลย
> หมายเหตุ: สคริปต์ทำงานกับ "สีฟ้า" ถ้ารันซ้ำหลังเปลี่ยนเป็นขาวแล้ว
> มันจะขึ้นว่าไม่พบสีฟ้าและข้ามไป — ถ้าจะเปลี่ยนสีใหม่ ให้กู้รูปเดิมจาก
> git ก่อน (`git checkout <commit เก่า> -- *-paddles.png`)

### สิ่งที่ต้องทำเอง
อัปโหลดทับ 3 ไฟล์: `standard-paddles.png`, `low-paddles.png`, `high-paddles.png`

---

## 2026-09-11 (รอบที่ 5) — พรีวิวคอนโทรลเลอร์จะไม่โผล่จนกว่าลูกค้าจะเลือกรุ่น

### ปัญหา
พอเข้าหน้า Custom Build มา **รูปคอนโทรลเลอร์โผล่ขึ้นมาเลยทันที**
ทั้งที่ลูกค้ายังไม่ได้เลือกว่าจะทำ PS4 หรือ PS5
รูปที่โชว์เป็นทรง DualShock 4 (PS4) ตลอด เลยกลายเป็นการชี้นำกลายๆ ว่า
"ร้านนี้ทำแต่ PS4" ทั้งที่จริงๆ รับทำ PS5 ด้วย

### แก้ยังไง
ซ่อนพรีวิวทุกแบบไว้ก่อน จนกว่าจะกดเลือกรุ่น — ในคอลัมน์นั้นมีพรีวิวซ้อนกันอยู่
**3 ชุด** (แบบ 3 มิติ, รูปแบนสำรอง, และรูป SVG) รอบนี้ปิดครบทั้งสามชุด
ไม่ใช่แค่ตัว 3 มิติ

ครอบคลุมกรณีที่ลูกค้ากด **"เปลี่ยนรุ่น"** ด้วย — พอการ์ดเลือกรุ่นเด้งกลับมา
พรีวิวจะซ่อนอีกครั้งจนกว่าจะเลือกใหม่ (ตรงนี้เช็คจากการ์ดที่โชว์อยู่
ไม่ได้ไปล้างค่ารุ่นที่เลือกไว้ เพราะค่านั้นยังต้องใช้คิดราคา)

> **สรุปราคายังโชว์ตามปกติ** ไม่ได้ถูกซ่อนไปด้วย — ทดสอบยืนยันแล้วทุกสถานะ

โหมด "เปลี่ยนอนาล็อกอย่างเดียว" ไม่มีขั้นเลือกรุ่น จึงไม่ถูกกระทบ

### ผลพลอยได้ — หน้าเว็บเปิดเร็วขึ้น
เมื่อก่อนไฟล์โมเดล 3 มิติ (6.9 MB) ถูกโหลดทิ้งตั้งแต่เปิดหน้า
ตอนนี้ **ยังไม่เลือกรุ่น = ยังไม่โหลดเลย (0 ไบต์)** ค่อยโหลดตอนเลือก PS4
คนที่เลือก PS5 หรือปิดหน้าไปก่อนก็ไม่ต้องเสียเน็ตฟรีๆ

### ผลทดสอบ
ไล่ทีละสถานะ ผ่านหมด ไม่มี error:

| สถานะ | พรีวิว | สรุปราคา |
|---|---|---|
| ยังไม่เลือกรุ่น | ซ่อน (โหลด 0 ไบต์) | โชว์ |
| เลือก PS4 | โชว์ 3 มิติ | โชว์ |
| สลับเป็น PS5 | ซ่อน | โชว์ |
| กด "เปลี่ยนรุ่น" | ซ่อน | โชว์ |
| กลับมา PS4 | โชว์ 3 มิติ | โชว์ |
| เปิดหน้าใหม่ | ซ่อน | โชว์ |

เช็คของเดิมซ้ำด้วยว่าไม่พัง — เปลี่ยนสีครบ 8 สี (สีเปลี่ยนจริง),
สลับ stick module / trigger / ปุ่มหลัง — พรีวิวอัปเดตตามปกติทุกอัน

### หมายเหตุเรื่องเลขเวอร์ชัน
เปลี่ยน `?v=3` -> **`?v=4`** เฉพาะ `controller-3d.js` กับ `controller-3d-glb.js`
ส่วน `ds4.glb` **คงเลข 3 ไว้เหมือนเดิม** เพราะไฟล์โมเดลไม่ได้แก้อะไร
ถ้าไปเปลี่ยนด้วยจะกลายเป็นบังคับให้ลูกค้าทุกคนโหลดใหม่ 6.9 MB ฟรีๆ

### สิ่งที่ต้องทำเอง
อัปโหลดทับแค่ **`index.html`** ไฟล์เดียว (ไฟล์อื่นไม่ได้แก้)

---

## 2026-09-11 (รอบที่ 4) — ลบโลโก้ให้ติดถาวรในไฟล์ + กันเบราว์เซอร์จำของเก่า

### ปัญหา
รอบที่แล้วลบโลโก้ "ตอนโหลดในเบราว์เซอร์" ซึ่งได้ผลก็จริง **แต่มีเงื่อนไขว่า
เบราว์เซอร์ต้องโหลดโค้ดตัวใหม่ก่อน** ถ้าเครื่องไหนยังจำไฟล์เก่าไว้ (cache)
ก็จะยังเห็นคำว่า SONY กับโลโก้ PlayStation อยู่เหมือนเดิม
— ซึ่งเป็นเรื่องที่เกิดขึ้นได้ง่ายมาก เพราะเบราว์เซอร์ชอบเก็บไฟล์ .js ไว้ใช้ซ้ำ

### แก้ยังไง — ลบให้ติดไปกับไฟล์เลย
เปลี่ยนจาก "ลบตอนโหลด" เป็น **ลบฝังลงไปในไฟล์ `models/ds4.glb` ถาวร**
คือแกะไฟล์ออกมา ลบโลโก้ในเทกซ์เจอร์ แล้วประกอบกลับเป็น .glb ใหม่
ทีนี้ไม่ว่าเบราว์เซอร์จะจำอะไรไว้ หรือใครเอาไฟล์ไปเปิดในโปรแกรม 3 มิติอื่น
**ก็ไม่มีโลโก้แล้ว** ไม่ต้องพึ่งโค้ดฝั่งเบราว์เซอร์อีก

เพิ่ม **`?v=3`** ต่อท้ายไฟล์ `controller-3d.js`, `controller-3d-glb.js`
และ `ds4.glb` ด้วย เพื่อบังคับให้เบราว์เซอร์โหลดของใหม่
> **เวลาแก้ไฟล์พวกนี้ครั้งต่อไป อย่าลืมเปลี่ยนเลข `?v=` เป็น 4, 5, ...**
> ลูกค้าจะได้เห็นของใหม่ทันที ไม่ต้องบอกให้กด Ctrl+F5

โค้ดลบตอนโหลดยังอยู่เหมือนเดิม (ตัวแปร `BRANDING`) ทำหน้าที่เป็นตาข่ายสำรอง
เผื่อวันหลังมีคนเอาไฟล์ .glb ต้นฉบับที่ยังมีโลโก้มาวางทับ

### ผลลัพธ์
- ไฟล์โต 6.85 MB -> 6.90 MB (+48 KB) เพราะเทกซ์เจอร์ 3 รูปถูกบีบใหม่
- จำนวนสามเหลี่ยม/ชิ้นส่วน/ขนาด เท่าเดิมทุกอย่าง (62,410 tri, 16.20 × 9.87 ซม.)
- ตรวจในไฟล์โดยตรง: กรอบที่ลบทุกจุด **ค่าความสว่างกระจาย = 0 (เรียบสนิท)**
- ไล่ 24 ชุดตัวเลือก / PS5 / lazy-load ซ้ำอีกรอบ — ผ่านหมด ไม่มี error

### สิ่งที่ต้องทำเอง
อัปโหลดทับให้ครบ: **`models/ds4.glb`** (ไฟล์ใหม่), `index.html`,
`controller-3d.js`, `controller-3d-glb.js`
ไฟล์ .glb เดิมสำรองไว้ที่เครื่องผมชั่วคราวเท่านั้น ถ้าอยากได้ของเดิมที่มีโลโก้
ต้องโหลดใหม่จาก Sketchfab

---

## 2026-09-11 (รอบที่ 3) — ลบยี่ห้อ SONY / โลโก้ PlayStation ออกจากโมเดล

### เปลี่ยนอะไร
ลบเครื่องหมายการค้าของ Sony ที่ติดมากับไฟล์โมเดลออกทั้งหมด 3 จุด

| จุด | อยู่ตรงไหน |
|---|---|
| คำว่า **SONY** | หลังเครื่อง ตรงคอจอยด้านบน |
| **โลโก้ PlayStation** | ปุ่ม PS ตรงกลางระหว่างก้านอนาล็อก |
| **ฉลากข้อกำหนด** | หลังเครื่อง — รุ่น CUH-ZCT2E, บาร์โค้ด, CE, EAC, MADE IN CHINA, ที่อยู่บริษัท |

ตอนนี้ตรงนั้นเป็นพลาสติกเรียบเปล่าๆ เปลี่ยนสีตามที่ลูกค้าเลือกได้ปกติ

### จุดที่พลาดง่าย — รอยไม่ได้อยู่แค่ในภาพสี
ตอนแรกลบแค่ "แผนที่สี" แล้วคิดว่าจบ แต่พอตรวจจริงพบว่าคำว่า SONY กับฉลาก
**ฝังอยู่ใน "แผนที่ความมัน" (roughness) ด้วย** ถ้าลบแค่สี เวลาไฟส่องกระทบ
จะเห็นเป็น**เงาจางๆ เป็นรูปตัวหนังสือ**ลอยอยู่บนผิว เหมือนรอยลบไม่สะอาด
เลยลบทั้งสองแผนที่พร้อมกัน (ตรวจแล้วว่าแผนที่ความนูน normal ไม่มีรอย จึงไม่ต้องแตะ)

วิธีลบไม่ได้ใช้การถมสีทับตายตัว แต่**เก็บสีพลาสติกจากขอบรอบๆ จุดนั้นมาเฉลี่ย
แล้วค่อยทาทับ** สีเลยกลืนกับผิวรอบข้างพอดี ไม่เป็นแผ่นปะ

### ตรวจแล้ว
- วัดค่าความสว่างในกรอบที่ลบ: จากเดิมกระจาย 134-150 ระดับ (มีตัวหนังสือ)
  เหลือ **0 = เรียบสนิท** ทั้งแผนที่สีและแผนที่ความมัน
- ดูด้วยตาทั้งมุมหลัง/บน/เอียง บนกรอบสีขาว (สีที่เห็นรอยชัดที่สุด) — ไม่มีเงาตกค้าง
- เปลี่ยนสีทับแล้วยังเรียบ ไม่มีรอยผุดกลับมา
- ไล่ 24 ชุดตัวเลือกซ้ำ — ไม่มี error, PS5 ยังซ่อนถูก, โหลดแบบ lazy ยังทำงาน
- ตัวหนังสือที่ **ไม่ใช่**ของ Sony ยังอยู่ครบ (SHARE / OPTIONS / EXT และช่องต่อต่างๆ)

### หมายเหตุ
ลบตอนโหลดในเบราว์เซอร์ **ไม่ได้แก้ไฟล์ `ds4.glb`** — ไฟล์ต้นฉบับยังเหมือนเดิม
ถ้าวันหลังอยากได้โลโก้กลับมา แก้ที่ตัวแปร `BRANDING` ใน `controller-3d-glb.js` ได้เลย
(เครดิต "Model created by shaielwolf" ยังต้องอยู่เหมือนเดิมนะครับ อันนั้นคนละเรื่องกัน
เป็นเงื่อนไขสัญญาอนุญาตของคนปั้นโมเดล ไม่ใช่ของ Sony)

---

## 2026-09-11 (รอบที่ 2) — เปลี่ยนไปใช้ไฟล์โมเดล DualShock 4 ของจริง

### เปลี่ยนอะไร
เดิมโมเดลเป็นทรงที่ "ปั้นด้วยโค้ด" เอง ถึงจะวัดแล้วเงาตรง 99.83% แต่มันก็ยังเป็น
ทรงเกลี้ยงๆ ไม่มีรายละเอียดผิวจริง ตอนนี้เปลี่ยนมาใช้ **ไฟล์โมเดล 3 มิติของจริง**
ที่สแกน/ปั้นมาครบทั้งร่องปุ่ม ตัวหนังสือ SHARE/OPTIONS/EXT ฉลาก SONY หลังเครื่อง
ไปจนถึงพื้นผิวทัชแพดแบบจุด

- **`models/ds4.glb`** (6.8 MB) — โมเดล *"DualShock 4 PlayStation Controller"*
  ของ **shaielwolf** จาก Sketchfab · 62,410 สามเหลี่ยม · เทกซ์เจอร์ 21 ไฟล์
  · สัญญาอนุญาต **CC BY 4.0** (ใช้เชิงพาณิชย์ได้ แต่**ต้องให้เครดิต**)
- **`vendor/GLTFLoader.js`** — ตัวอ่านไฟล์ .glb
- **`controller-3d-glb.js`** — ไฟล์ใหม่ ทำหน้าที่โหลด/จัดแกน/ย้อมสีโมเดล

### เครดิตเจ้าของโมเดล — ห้ามลบ
มุมขวาล่างของกล่องพรีวิวมีข้อความ **"Model created by shaielwolf"** กดแล้วลิงก์ไป
หน้าโมเดลต้นทาง อันนี้เป็น**เงื่อนไขบังคับของสัญญาอนุญาต CC BY 4.0** ถ้าลบออก
= ใช้งานผิดสัญญา กรุณาอย่าเอาออกครับ

### เรื่องที่ยากที่สุด — ทำไมสีกรอบถึงเปลี่ยนไม่ได้ตอนแรก
เทกซ์เจอร์ที่มากับโมเดล **ทาสีดำมาแล้ว** (จอยต้นฉบับเป็นสีดำ) ปกติการเปลี่ยนสี
ในงาน 3 มิติคือเอาสีไป "คูณ" กับเทกซ์เจอร์ แต่ **อะไรคูณกับสีดำก็ได้สีดำ**
ต่อให้สั่งเป็นสีขาวก็ยังได้จอยดำอยู่ดี ลูกค้าจะไม่เห็นสีที่เลือกเลย

วิธีแก้: **วาดเทกซ์เจอร์ขึ้นมาใหม่ทีละพิกเซล** ตอนที่ลูกค้าเลือกสี
- พิกเซลที่เป็นเนื้อพลาสติก → เปลี่ยนเป็นสีที่เลือก (ไล่เฉดตามแสงเงาเดิม
  โมเดลเลยยังดูมีมิติ ไม่แบนเป็นสีเดียว)
- พิกเซลที่เป็นตัวหนังสือ/ขีดขาว → คงไว้ ทำให้คำว่า SHARE / OPTIONS / EXT
  และฉลากหลังเครื่องยังอ่านออกครบเหมือนของจริง

ใช้หลักเดียวกันกับก้านอนาล็อกและปุ่ม (แต่ตั้งให้สีเข้มกว่า เพราะเป็นชิ้นเล็ก
ถ้าจางจะดูไม่ออกว่าเลือกอะไรไป)

**ไลต์บาร์** ก็ได้ของแถม — โมเดลมีไฟอบมาในตัวอยู่แล้ว เลยวาดทับเป็นสีของบอร์ด
PCB ที่เลือก ได้ไฟที่รูปทรงถูกต้องกว่าเดิมที่แปะแท่งเรืองแสงเอง

### เน็ตไม่ช้าลง
ไฟล์ 6.8 MB จะ**ยังไม่โหลดจนกว่าลูกค้าจะเลื่อนมาเห็นกล่องพรีวิวจริงๆ**
คนที่เข้ามาดูหน้าแรกแล้วออก จะไม่โดนโหลดไฟล์นี้เลย (ตรวจแล้วว่าไม่โหลดจริง)
และถ้าโหลดไฟล์ไม่สำเร็จ **จะตกกลับไปใช้โมเดลที่ปั้นด้วยโค้ดตัวเดิมอัตโนมัติ**
ลูกค้าไม่มีทางเจอกล่องว่าง

### ตรวจแล้ว (รันจริงบน Chromium)
- ขนาดโมเดลหลังจัดสเกล **16.20 × 9.87 ซม.** (ของจริง 16.2 × 9.8) — ตรงสเปก
- สีครบทั้ง 8 สี เปลี่ยนได้จริงทุกสี (Crystal ยังใสอยู่) ใช้เวลาเปลี่ยนสี ~48 มิลลิวินาที
- ไล่ทดสอบ **24 ชุดตัวเลือก** (สี × ก้านอนาล็อก × ปุ่มหลัง × ความสูง × trigger) — ไม่มี error
- ปุ่มหลังเกาะด้ามจับถูกตำแหน่ง (วัดจากผิวจริงของโมเดล ไม่ได้กะเอา)
- PS5 ซ่อนโมเดลถูกต้อง · กลับมา PS4 แล้วโมเดลกลับมา
- มือถือ 390px ไม่ล้นจอ เครดิตอยู่ในกรอบพอดี

### สิ่งที่ต้องทำเอง
อัปโหลดไฟล์ใหม่ขึ้นโฮสต์ให้ครบ: **`models/ds4.glb`**, **`vendor/GLTFLoader.js`**,
**`controller-3d-glb.js`** และไฟล์ที่แก้ **`controller-3d.js`**, **`index.html`**
(โฟลเดอร์ `models/` กับ `vendor/` ต้องมีจริงบนโฮสต์ ไม่งั้นจะตกไปใช้โมเดลสำรอง)

---

## 2026-09-11 — แก้ทรงโมเดล 3 มิติ ให้ตรงกับ DualShock 4 ของจริง

### ปัญหา
รอบที่แล้วปั้นทรงจอยด้วยการ "กะสัดส่วนเอาเอง" ผลคือทรงเพี้ยนจาก DS4 จริงหลายจุด
ที่ผิดหนักสุดคือ **ปั้นให้กว้างสุดตรงบ่า (ฝั่งปุ่มไหล่)** ทั้งที่ DS4 ของจริง
**กว้างสุดตรงด้ามจับ** ส่วนขอบบนฝั่งไลต์บาร์กว้างแค่ ~12.2 ซม. เท่านั้น
พอทรงพื้นฐานผิด ต่อให้ขยับปุ่มยังไงก็ไม่มีทางเหมือน

### วิธีแก้ — เลิกเดา ใช้เส้นจริงจากไฟล์ในโปรเจกต์
ในโปรเจกต์มี **`ds4-color-tool.html`** ซึ่งข้างในเป็นไฟล์เวกเตอร์ DualShock 4
ที่เขียนไว้ละเอียดมาก (แยกชิ้นส่วนครบ มี id กำกับทุกชิ้น) เลยดึงพิกัดจริงจากไฟล์นั้น
มาใช้แทนการกะเอง:

- **เส้นขอบตัวเครื่อง** — ลอกจาก path `rect4272-5-1-3` โดยตรง แล้วสเกลให้กว้าง
  16.2 ซม. ตามสเปกจริง (162 × 98 มม.) ได้ผลออกมา **16.18 × 9.79 ซม.**
- **ตำแหน่งชิ้นส่วนทุกชิ้น** — วัดจากไฟล์เวกเตอร์แล้วย้ายตามจริงทั้งหมด:
  ทัชแพด, ไลต์บาร์ (กว้าง 5.08), L1/R1, L2/R2, D-pad (ย้ายไป x=-5.07 z=-2.33),
  ปุ่มหน้า 4 ปุ่ม (x=5.09, ระยะห่าง 1.18, หัวปุ่ม Ø1.01), ก้านอนาล็อก (x=±2.59),
  ปุ่ม PS, ลำโพง, Share/Options
- **เพิ่มแอ่งวงกลมรอบปุ่มหน้า** ให้เหมือนฝั่ง D-pad (ของจริงมีทั้งสองฝั่ง)
- **ทรงด้ามจับ** — เดิมเป็นก้อนกลมๆ ที่เดาตำแหน่ง เปลี่ยนมาพองตาม "แกนด้ามจับ"
  ที่คำนวณจากเงาจริง (เอียงจากแกนกลาง 8.6°) ทำให้ด้ามจับวางตัวถูกทิศ
- **ย้ายปุ่มหลัง** ให้เกาะสันด้ามจับใหม่ (ของเดิมลอยพ้นตัวเครื่องเพราะทรงเปลี่ยน)

### ตรวจแล้วว่าเหมือนจริงแค่ไหน (วัดเป็นตัวเลข ไม่ใช่กะด้วยตา)
- **เงาตัวเครื่องทับกับไฟล์เวกเตอร์จริง 99.83%** (IoU)
- จุดที่เลยขอบจริงมากสุด **0.009 ซม.** — คือแทบไม่เกินเลย
- ความกว้างทุกระดับความลึกคลาดเคลื่อนไม่เกิน **0.11 ซม.**
- ความหนา: กลางเครื่อง 2.39 ซม. · ด้ามจับ 4.69 ซม. (ของจริง ~2.6 / ~4.5-5.0)
- เร็วขึ้นด้วย: สร้างโมเดล **110 มิลลิวินาที** (เดิม 186), 38,660 สามเหลี่ยม

### ทดสอบซ้ำทั้งชุด — ผ่านหมด
รันจริงบน Chromium: โมเดลขึ้น ไม่มี JS error · สี 8 สีถูกทุกสี · ปุ่มหลังโชว์ถูกด้าน
และไม่โผล่พ้นตัวเครื่อง · PS5 ซ่อนโมเดลถูกต้อง · มือถือ 390px ไม่ล้นจอ ·
เครื่องไม่มี WebGL ยังตกไปใช้รูปแบนเดิมได้ปกติ ฟอร์มคิดราคาได้เหมือนเดิม

### สิ่งที่ต้องทำเอง
อัปโหลด **`controller-3d.js`** ทับของเดิมบนโฮสต์ (ไฟล์อื่นไม่ได้แก้)

---

## 2026-09-10 — พรีวิวคอนโทรลเลอร์ 3 มิติ (หมุนได้) แทนรูปแบน

### ไฟล์ใหม่
- **`controller-3d.js`** — โมเดล DualShock 4 แบบ 3 มิติ + ตัวแสดงผล (three.js) **ปั้นด้วยโค้ดล้วน ไม่มีไฟล์โมเดล .glb/.fbx ให้ต้องอัปโหลด** (เว็บนี้เป็น static HTML ไม่มี build step — การเพิ่มไฟล์โมเดลก้อนใหญ่จะยุ่งกว่า)
- **`vendor/three.min.js`** — three.js r149 เก็บไว้ในเครื่อง (ถ้าไฟล์นี้หาย โค้ดจะไปโหลดจาก CDN ให้เองอัตโนมัติ)

### หน้าเว็บลูกค้า — `index.html`
- เพิ่มกล่อง **"Controller Preview · 3D"** ไว้บนสุดของแผงสรุปด้านข้าง — ลากหมุนได้ 360°, ซูมได้ (ล้อเมาส์/นิ้วหนีบ), มีปุ่มมุมกล้อง 4 มุม (มุมเอียง / ด้านบน / ด้านหน้า / **ปุ่มหลัง** = พลิกดูใต้เครื่อง) และปุ่มเปิด-ปิดหมุนอัตโนมัติ
- โมเดลอัปเดต **เรียลไทม์ตามตัวเลือกในฟอร์ม**: สีกรอบทั้ง 8 สี (Crystal เป็นพลาสติกใสจริงๆ), ชนิดก้านอนาล็อก (วงแหวนเปลี่ยนสี ALPS/HE/TMR), Trigger/Face Clicky (ชิ้นที่อัปเกรดเรืองเป็นสีเขียวมะนาวของร้าน), ปุ่มหลัง (จำนวน/ด้านซ้าย-ขวา/ระดับความสูง 3 ระดับเห็นต่างกันจริง), บอร์ด PCB (สีไฟ Light Bar)
- มีป้ายสรุปสเปกใต้โมเดลบอกว่าตอนนี้เลือกอะไรอยู่บ้าง
- **ไม่ได้แตะ logic การเลือกของ/ราคา/การส่งออเดอร์เลย** — เพิ่มแค่ตัวแสดงผล อ่านค่าจากฟังก์ชันเดิม (`getSelected`, `getPaddlesValue`, `getShellColorValue` ฯลฯ) ผ่าน `rerender()` ที่มีอยู่แล้ว

### กันพัง (fallback)
- เครื่องไม่รองรับ WebGL / โหลด three.js ไม่ได้ → ซ่อนกล่อง 3 มิติ แล้วโชว์รูปแบนเดิมกลับมาอัตโนมัติ (ลูกค้าไม่เห็นกล่องว่าง)
- เลือก **PS5 (DualSense)** → ซ่อนโมเดล 3 มิติ เพราะโมเดลนี้เป็นทรง DualShock 4 ทรงไม่ตรงรุ่น (ตกไปใช้รูปแบนเดิม) — ถ้าอยากได้ทรง DualSense ด้วยต้องปั้นเพิ่ม
- โหมด "เปลี่ยนอนาล็อกอย่างเดียว" → ซ่อนโมเดล (ไม่ได้แตะตัวเครื่อง)
- หยุดวาดภาพเมื่อเลื่อนพ้นจอ/สลับแท็บ เพื่อไม่ให้กินแบตมือถือฟรีๆ

### ทดสอบแล้ว
- รันจริงบน Chromium (WebGL/SwiftShader): โมเดลขึ้นจริง, สีถูก, ลากหมุนได้, ปุ่มมุมกล้องทำงาน, ไม่มี JS error
- ตรวจ logic ครบทุกตัวเลือก (สี 8 สีไม่ซ้ำกัน, ปุ่มหลังโชว์ถูกด้าน, ความสูง 3 ระดับต่างกันจริง, ไม่มีจุดพิกัดเสีย, สลับค่าซ้ำๆ 40 รอบไม่มี memory leak)
- ขนาดโมเดลตรงของจริง ~16.5 × 10.9 ซม. (DS4 จริง 16.2 × 9.8 ซม.)
- มือถือ (390px): ไม่ล้นจอ ไม่ทับแถบราคาลอย

### สิ่งที่ต้องทำเอง
1. **อัปโหลดไฟล์ใหม่ 2 ไฟล์ขึ้นโฮสต์ด้วย**: `controller-3d.js` และโฟลเดอร์ `vendor/` (ถ้าลืม `vendor/` จะยังใช้ได้แต่ต้องโหลด three.js จาก CDN ทุกครั้ง = ช้ากว่า และถ้าลูกค้าเน็ตบล็อก CDN จะตกไปใช้รูปแบนเดิม)
2. ไม่ต้อง deploy Apps Script ใหม่ — รอบนี้ไม่ได้แตะไฟล์ `.gs`

---

## 2026-08-03 — ระบบ login ลูกค้า + ลืมรหัสผ่าน (deploy-ready)

### หน้าเว็บลูกค้า — `index.html`
- ยืนยัน login flow ทั้งหมด (สมัครสมาชิก/เข้าสู่ระบบ/ออกจากระบบ/คำสั่งซื้อของฉัน) มีอยู่ครบแล้ว
- เพิ่มปุ่ม **"ลืมรหัสผ่าน?"** ในหน้าเข้าสู่ระบบ (ซ่อนอัตโนมัติเมื่อสลับโหมดสมัครสมาชิก)
- เพิ่มหน้า **"ลืมรหัสผ่าน"** — กรอกอีเมลแล้วส่งลิงก์รีเซ็ตทางอีเมล
- เพิ่มหน้า **"ตั้งรหัสผ่านใหม่"** — รองรับ URL `?resetToken=...` จากลิงก์ในอีเมล (ลบ param ออกจาก URL อัตโนมัติ)
- เพิ่มส่ง `authEmail` + `authToken` ไปพร้อม payload ตอนยืนยันคำสั่งซื้อ — backend ผูกออเดอร์กับบัญชีอัตโนมัติถ้า login อยู่

### แบ็กเอนด์ — `apps-script-order-receiver.gs`
- ยืนยัน action `register`, `login`, `logout`, `getMyOrders` มีอยู่ครบ
- เพิ่ม action **`forgotPassword`** — สร้าง reset token, บันทึกลงชีต `PasswordResets`, ส่งอีเมลผ่าน `MailApp`
- เพิ่ม action **`resetPassword`** — ตรวจ token, อัปเดตรหัสผ่าน (hash+salt ใหม่), มาร์ค token ว่าใช้แล้ว
- เพิ่มชีต **`PasswordResets`** (สร้างอัตโนมัติ): Token | อีเมล | สร้างเมื่อ | หมดอายุเมื่อ | ใช้แล้ว
- เพิ่ม `SITE_URL` config — ใส่ URL จริงของ index.html เพื่อให้ลิงก์ในอีเมลคลิกได้

### สิ่งที่ต้องทำเอง (ก่อนใช้งานจริง)
1. **Deploy Apps Script ใหม่** — `.gs` แก้ไขแล้ว ต้อง deploy ก่อน
2. **ตั้งค่า `SITE_URL`** ในไฟล์ `.gs` ใส่ URL จริงของ index.html เพื่อให้ลิงก์รีเซ็ตในอีเมลคลิกได้
3. ชีต `PasswordResets` สร้างอัตโนมัติตอนมีคนขอลืมรหัสผ่านครั้งแรก ไม่ต้องสร้างมือ

---

## 2026-07-31 — เพิ่มระบบ login ลูกค้า + หน้า "คำสั่งซื้อของฉัน"

### หน้าเว็บลูกค้า — `index (9).html`
- เพิ่มปุ่ม "เข้าสู่ระบบ" ที่ top-bar มุมขวาบน — **ไม่บังคับ** ต้อง login ก่อนสั่งซื้อ ยังสั่งซื้อแบบ guest ได้ตามปกติทุกประการเหมือนเดิม
- เพิ่มหน้าจอเข้าสู่ระบบ/สมัครสมาชิก (อีเมล + รหัสผ่านต่อบัญชี มีแท็บสลับ)
- เพิ่มหน้า "คำสั่งซื้อของฉัน" — ประวัติคำสั่งซื้อทั้งหมดของอีเมลที่ login อยู่ (วันที่, เลขที่คำสั่งซื้อ, รายการ, ยอดรวม, badge สถานะการชำระเงิน: ยืนยันแล้ว/ต้องตรวจสอบ/ยกเลิก/รอชำระ)
- เก็บ session (email/name/token) ไว้ใน `localStorage` คีย์ `mosuAuth` ให้ล็อกอินค้างข้ามการรีเฟรชหน้า

### แบ็กเอนด์ — `apps-script-order-receiver.gs`
- เพิ่มชีตใหม่ (สร้างอัตโนมัติตอนเรียกใช้ครั้งแรกเหมือนชีตอื่นๆ): **Users** (อีเมล/ชื่อ/รหัสผ่านแฮช SHA-256+salt ต่อคน/วันที่สมัคร — ไม่เก็บรหัสผ่านจริง), **Sessions** (token/อีเมล/วันที่สร้าง — หมดอายุอัตโนมัติ 30 วัน)
- เพิ่ม action ใหม่: `register`, `login`, `logout` (POST), `getMyOrders` (GET — ต้องแนบ email+token จาก login/register เท่านั้น ไม่งั้นดึงประวัติคนอื่นไม่ได้)
- `getMyOrders` จับคู่ด้วยคอลัมน์ "อีเมล" ในชีต Orders ตรงๆ — ออเดอร์เก่าที่เคยสั่งแบบ guest (ก่อนสมัครสมาชิก) จะโผล่มาอัตโนมัติถ้ากรอกอีเมลเดียวกัน ไม่ต้อง migrate ข้อมูลเก่า

### สิ่งที่ต้องทำเอง (ทำค้างไว้ไม่ได้ ระบบใช้งานไม่ได้จนกว่าจะทำ)
1. **Deploy Apps Script ใหม่** (วาง `apps-script-order-receiver.gs` ทับของเดิมใน Apps Script Editor → Deploy → Manage deployments → แก้ deployment เดิม → Deploy) — ไม่งั้นปุ่ม login/สมัครสมาชิก/คำสั่งซื้อของฉันจะขึ้น error เชื่อมต่อไม่สำเร็จ
2. รหัสผ่านเก็บแบบแฮชพื้นฐาน (ไม่มี rate-limit/2FA/ลืมรหัสผ่าน) เหมาะกับร้านขนาดนี้ — ถ้าลูกค้าลืมรหัสผ่าน ตอนนี้ต้องให้แอดมินลบแถวในชีต Users แล้วให้ลูกค้าสมัครใหม่เอง (ยังไม่มีหน้า "ลืมรหัสผ่าน")

---

## 2026-07-30 — เพิ่ม hook เตือนอัปเดต CHANGELOG.md อัตโนมัติ

### เครื่องมือ/config — `.claude/settings.json` (ไฟล์ใหม่)
- เพิ่ม Stop hook — ทุกครั้งที่ Claude ตอบจบ จะเช็คว่าไฟล์หลัก (`index (9).html`, `dashboard.html`, `apps-script-order-receiver.gs`) ถูกแก้หลังจากอัปเดต `CHANGELOG.md` ครั้งล่าสุดหรือไม่ ถ้าใช่ จะเตือนให้ Claude ไปเพิ่มบันทึกก่อนจบงานจริง (ไม่ใช่แค่ข้อความเตือนเฉยๆ — บล็อกไม่ให้จบจนกว่าจะจัดการ)
- ทดสอบแล้วว่า logic ทำงานถูกต้องทั้ง 2 ทาง (ไฟล์ไม่เปลี่ยน = ไม่เตือน, ไฟล์เปลี่ยน = เตือน) ผ่านการจำลอง stdin/JSON output ตรงๆ

### สิ่งที่ต้องทำเอง
1. **hook นี้จะเริ่มทำงานจริงในบทสนทนาใหม่** (session ปัจจุบันเปิดมาก่อนไฟล์ settings นี้จะถูกสร้าง เลยยังไม่เห็นการเปลี่ยนแปลงนี้จนกว่าจะเริ่ม session ใหม่ในโฟลเดอร์นี้)
2. ดู/แก้/ปิด hook นี้ได้ที่ `.claude/settings.json`

---

## 2026-07-30 — แผงควบคุมแอดมิน (dashboard.html), ต้นทุนแบบช่วง, ตัวเลือกสีกรอบ PS4, แก้บั๊กหลายจุด

### หน้าเว็บลูกค้า — `index (9).html`
- เพิ่มตัวเลือก "ไม่มีจอย ให้เลือกสี" ในช่อง 05 (กรอบคอนโทรลเลอร์ PS4) — ปุ่มเลือกสี 8 สี ใช้รูปจากโฟลเดอร์ `DS4 COLOR/*_norm.png` (ขนาด/สัดส่วนเท่ากันหมด) สีที่เลือกจะโชว์รูปจริงคู่กับรูป PCB ในแผง "Component Preview" และผูกกับ Controller Preview (SVG) ให้ขึ้นชื่อสีถูกต้อง
- เพิ่มระบบเตือน "เลือกของไม่ครบ" ทุกขั้นของวิซาร์ด (ไม่ใช่แค่ตอนกด "ยืนยันคำสั่งซื้อ" ตอนจบ) — ปุ่มหลัง, Trigger/Face, Stick Module, กรอบ PS4 (+สีถ้าเลือกไม่มีจอย) ต้องเลือกครบก่อนไปขั้นถัดไป
- เพิ่มปุ่ม "← กลับหน้าหลัก" ที่ขั้นที่ 1 ของวิซาร์ด (กลับไปหน้าเลือกโหมด Custom Build / เปลี่ยนอนาล็อก)
- รองรับฟีเจอร์ "เปิดขาย" จากชีต Prices — ตัวเลือกที่ปิดขาย (พิมพ์ "ปิด" ในชีต) จะหายไปจากหน้าเว็บทั้งหัวข้อ (ต่างจาก "คงเหลือ"=0 ที่ยังโชว์แต่ขึ้น "สินค้าหมด")
- แก้บั๊ก CSS: `.option`/`.pcb-col` มี `display` กำหนดตายตัวจนทับ attribute `hidden` ของเบราว์เซอร์ ทำให้ซ่อนตัวเลือกไม่ได้จริง (เพิ่ม `[hidden] { display:none }` ให้ทั้งคู่)
- Mobile: ปุ่มเลือกสีกรอบ ปรับให้เรียง 3 ต่อแถวบนจอเล็ก (เดิม 2)

### แผงควบคุมแอดมิน — `dashboard.html` (ไฟล์ใหม่ ไม่ลิงก์จากหน้าเว็บลูกค้า)
หน้าเว็บแยกต่างหาก ต้องกรอกรหัสผ่าน (`DASHBOARD_PASSWORD` ในไฟล์ .gs) ก่อนใช้งาน มี 3 แท็บ:
- **กำไร** — สรุปยอดขาย/ต้นทุน/กำไรรายเดือน แยก 4 กลุ่มตามสถานะจ่ายเงิน: ยืนยันแล้ว (✓) / ต้องตรวจสอบ (⚠) / ยกเลิก (✕) / รอชำระ — กันตัวเลขกำไรเพี้ยนจากออเดอร์ที่ยังไม่จ่ายจริง
- **ราคา/สินค้า** — แก้ราคา/ต้นทุน/คงเหลือ/เปิดขาย ของทุกตัวเลือกได้จากหน้าเว็บโดยตรง ไม่ต้องเปิด Google Sheets เอง (การ์ดแยกตามหมวด แก้แล้วกด "บันทึก" ทีละใบ)
- **ออเดอร์** — ดูรายการคำสั่งซื้อ/รายละเอียด/ที่อยู่/สลิป และแก้ "สถานะการชำระเงิน" ด้วยมือได้ (เช่นยืนยันเอง หรือยกเลิกออเดอร์)

### ต้นทุนแบบช่วง (ไม่นิ่ง)
- ต้นทุนกรอกเป็นช่วงได้ (เช่น 1600–1700) แทนตัวเลขเดียว — คอลัมน์ Prices เดิม (F) = ต้นทุนต่ำสุด, เพิ่มคอลัมน์ H = ต้นทุนสูงสุด (เว้นว่าง = ไม่ใช้ช่วง เหมือนเดิม)
- ต้นทุนที่ใช้คำนวณกำไรจริงทุกจุด = **ค่ากลางของช่วง** เสมอ

### บั๊กสำคัญที่เจอและแก้
- **ชีต Orders จริงของร้านใช้ชื่อคอลัมน์เก่า** (เช่น "รออกค้าชำระเงิน ( PAYMENT CONFIRM )" แทน "สถานะการชำระเงิน", "EMAIL" แทน "อีเมล") ทำให้แดชบอร์ดเคยขึ้น ฿0/สถานะว่างเปล่าทุกแถว — แก้โดยให้ระบบลองหาชื่อคอลัมน์หลายแบบ (ทั้งฝั่งอ่านและฝั่งเขียน) กันพังจากชื่อคอลัมน์ไม่ตรงเป๊ะ

### สิ่งที่ต้องทำเอง (ทำค้างไว้ไม่ได้ ระบบใช้งานไม่ได้จนกว่าจะทำ)
1. **Deploy Apps Script ใหม่** ทุกครั้งที่แก้ไฟล์ `.gs` (Apps Script Editor → Deploy → Manage deployments → แก้ deployment เดิม → Deploy) — หน้าเว็บ/แดชบอร์ดจะยังคุยกับโค้ดเวอร์ชันเก่าจนกว่าจะ deploy
2. **เปลี่ยน `DASHBOARD_PASSWORD`** ในไฟล์ .gs จากค่า placeholder ก่อนใช้งานจริง — endpoint เป็น URL สาธารณะ รหัสผ่านคือด่านกันเดียว ห้ามแชร์ไฟล์ dashboard.html หรือรหัสผ่านให้ใคร
3. คอลัมน์ใหม่ในชีต Prices ("เปิดขาย", "ต้นทุนสูงสุด") จะถูกเพิ่มให้อัตโนมัติเองตอนโหลดหน้าเว็บ/แดชบอร์ดครั้งแรกหลัง deploy ไม่ต้องไปสร้างเองในชีต

---

<!--
วิธีเขียนรอบถัดไป: เพิ่ม section ใหม่ไว้บนสุด (ใต้บรรทัด "ใหม่อยู่บนสุด") รูปแบบ:

## YYYY-MM-DD — หัวข้อสั้นๆ สรุปว่าทำอะไร

### [ชื่อไฟล์หรือฟีเจอร์]
- เปลี่ยนอะไร (บรรทัดเดียวจบต่อประเด็น)

### สิ่งที่ต้องทำเอง (ถ้ามี)
1. ...
-->
